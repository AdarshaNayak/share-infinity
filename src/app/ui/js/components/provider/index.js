import providerTemplate from "./provider.html";
import providerController from "./provider-controller";

export const providerComponent = {
    template: providerTemplate,
    controller: providerController,
    controllerAs: "$ctrl"
};
